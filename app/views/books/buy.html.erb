<h3 class="float-end"><%= link_to 'Export as pdf', books_export_path(format: :pdf), class: 'btn btn-primary' %></h3>

<% @books.each do |book| %>
  <%= render partial: 'book_details', locals: { book: book } %>
<% end %>