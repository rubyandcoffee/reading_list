<h3>My Shopping List</h3>
<% @books.sort_by { |b| b.author.surname }.each do |book| %>
  <p>
    <%= book.title %> by <%= book.author.full_name %>
    <% if book.current_state == 'read' %>
      (read)
    <% end %>
  </p>
<% end %>